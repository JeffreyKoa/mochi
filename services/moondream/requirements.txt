# transformers 后端（默认）最小依赖；torch 由 start.ps1 按 GPU 安装 CUDA/CPU
transformers>=4.44.0,<4.50.0
accelerate>=0.26.0
einops>=0.7.0
pillow>=10.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
# photon 后端可选：pip install moondream timm
