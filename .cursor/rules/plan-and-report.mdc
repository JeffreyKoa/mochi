---
description: 任务开始前写计划待确认，执行后写执行结果与完成度
alwaysApply: true
---

# 计划 → 确认 → 执行 → 汇报

## 每日文件夹（必须）

每个工作日**第一次**处理开发类任务前，在 `docs/` 下按当天日期新建文件夹：

```
docs/YYYYMMDD/
```

示例：`docs/20260723/`

- 格式固定为 **8 位数字** `YYYYMMDD`，无分隔符
- 若当天文件夹已存在则复用，不重复创建
- 每个任务以**计划标题**命名文件（见下方命名规范）
- 同一日期文件夹内可并存多个任务的计划与结果

### 文件命名

先确定**计划标题**（简短概括本次任务，作为文档 `#` 一级标题），文件名由此派生：

| 类型 | 路径 |
|------|------|
| 计划 | `docs/YYYYMMDD/{计划标题}.md` |
| 执行结果 | `docs/YYYYMMDD/{计划标题}-执行结果.md` |

示例：标题为「实时对话延迟优化 Phase1」→

```
docs/20260723/实时对话延迟优化 Phase1.md
docs/20260723/实时对话延迟优化 Phase1-执行结果.md
```

命名规范：去掉 `\ / : * ? " < > |` 等非法字符；过长时可缩写，但须能识别任务。

## 开始前：写计划，待确认

收到**实现、修复、重构、迁移**类任务后，**先写计划，不要直接改代码**（除非用户明确说「直接做 / 不用确认」）。

计划须包含：

0. **标题**：一级标题 `# {计划标题}`，与文件名一致
1. **目标**：要解决什么问题
2. **范围**：涉及哪些文件/模块
3. **步骤**：按顺序列出具体动作
4. **风险/依赖**：可能阻塞项（权限、环境、第三方 API 等）
5. **验收标准**：怎样算完成

确认前将计划**同步写入** `docs/YYYYMMDD/{计划标题}.md`。

计划末尾固定写：

> **请确认以上计划，确认后我再开始执行。**

以下情况可跳过计划，直接执行：

- 用户明确说「直接做 / 不用计划 / 不用确认」
- 纯问答、代码解释、Review（不涉及改代码）
- 单行 typo 等 trivial 修复

## 执行后：写执行结果

任务执行完毕（或中途被阻塞停止）后，**必须**输出 **「执行结果」** 小节，格式如下：

```markdown
## 执行结果

### 已完成
- [x] 步骤 1：……
- [x] 步骤 2：……

### 未完成
- [ ] 步骤 3：……（原因：……）

### 完成度
**XX%**（已完成 N / 计划 M 项）

### 建议
- ……
```

要求：

- **已完成 / 未完成** 须与计划步骤一一对应
- **完成度** 用百分比 + 分数（如 3/4 项）
- **建议** 写后续行动：待用户决策项、技术债、测试/部署注意点等；无则写「无」

同时将执行结果**同步写入** `docs/YYYYMMDD/{计划标题}-执行结果.md`。

若执行中发现计划需调整，先说明变更理由，再继续或等待确认。
